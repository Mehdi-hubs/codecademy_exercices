const kelvin = 0;

// We create a constant variable "kelvin" that gives the daily forecast.

let celsius = kelvin - 273;

// 'celcius' is a new variable that converts the inital forecast value from Kelvin to Celsius degrees.

let farenheit = celsius * (9 / 5) + 32;

// 'Farenheit' is a new variable that converts the celsius forecast value to Farenheit degrees.

farenheit = Math.floor(farenheit);

// Rounds down the farenheit variable.

console.log(`The temperature is ${farenheit} degrees farenheit.`);

// Displays the converted result.

newton = celsius * (33 / 100);

newtown = Math.floor(newton);

console.log(`The temperature is ${newton} degrees Newton.`);
